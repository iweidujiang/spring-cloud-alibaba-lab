server:
  port: 7071

spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
  config:
    import:
      - nacos:test.yml
